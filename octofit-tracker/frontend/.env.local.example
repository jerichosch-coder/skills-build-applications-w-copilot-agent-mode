# VITE_CODESPACE_NAME must be defined for the frontend to reach the backend API in Codespaces.
# Copy this file to .env.local and set the value (do not commit .env.local).
# In GitHub Codespaces, run: echo $CODESPACE_NAME
VITE_CODESPACE_NAME=your-codespace-name-here
